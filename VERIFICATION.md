# 验证记录

## 0.7.0 原尺寸、标尺与联合排版（2026-09-21）

- 45项单元测试通过。新增真实像素比例、不同校准倍率、已校准/未校准分开分页、200对象分页/无重叠/页脚避让、整组缩小、独立标尺换算以及页码/设置验证。
- `tests/plate07.smoke.mjs` 使用MR0415_Image018.jpg与MR0415_Image026.jpg的13枚候选：共同比例0.5版面单位/原像素，即2倍PNG中1:1；来源A/B、2页分组排版、紧凑混排、参与照片选择、整套PNG+来源清单导出、标尺开关、逐枚缩放后各自标尺、项目和重启恢复均通过。
- 两张实际照片没有已知尺度，因此测试输出仅标px，未人为赋予μm。μm换算通过独立几何单元测试和已校准合成图GUI检查；不同拍摄倍率的真实物理校准仍需用户提供。
- 200对象测试检查分组及紧凑两模式均无对象丢失、无旋转包围盒/标签区重叠、不进入页脚，原始尺寸共同倍率不随页面改变。没有把启发式布局宣称为最优装箱。
- 原有13组GUI检查与0.6的5组真实图排版检查通过，保留校准、mask编辑、长轴转正、逐枚移动/旋转/缩放、黑白背景及保存恢复功能。
- 1500×1000及1040×720截图已查看。测试勾选仅用于验证，非专家完整性确认。旧应用及源照片未改动；Windows、Intel Mac、原生WebView仍未实机验证。

## 0.6.0 排版转正与背景验证（2026-09-21）

- 38项单元测试通过。新测：不同倾角mask主轴、竖直转正幂等性、近圆/空mask不定向、旋转后占用范围和水平标签边界、旋转状态下统一倍率、背景与长轴字段兼容及无效坐标保护。
- `tests/plate06.smoke.mjs` 使用原始 MR0415_Image018.jpg：4枚候选，稳定几何长轴均转为竖直；旋转与移动不修改任何透明裁剪的PNG内容。不是生物学头尾识别或普适精度评估。
- 5组新增端到端检查通过：默认转正；逐枚拖动/角度输入/翻转180°/缩放/撤销；黑白预览与PNG；独立JSON、项目ZIP、重启恢复；旧版JSON补算长轴及白底默认值。
- 黑底、白底PNG均为2400×1800，角落像素分别为不透明[0,0,0]和[255,255,255]；标签颜色对应切换。白底转正、黑底手调截图已实际查看。
- 修复DOMPoint展开未得到x/y导致的无效拖动位置；现在明确读取坐标，移动后检查全部位置/尺寸为有限数，并在保存/导出前校验版面。
- 原有13组浏览器回归检查全部通过，包括几何校准、mask编辑、逐枚确认、统一倍率与缩放切换、PNG/CSV/ZIP导出、项目恢复、TIFF。
- 原始照片与旧应用保留。测试项目中的勾选只是测试，不等同于专家完整性确认。Windows、Intel Mac及原生WebView未实机验证。

## 0.5.0 界面与恢复验证（2026-09-21）

- 33项单元测试通过，新增统一物理倍率、组合移动页边约束和对齐/等距检查。分割参数与算法未更改。
- 新增 `tests/workspace.smoke.mjs`：使用原始 MR0415_Image026.jpg，8组界面验证全部通过，测试浏览器无运行错误。
- 验证：侧栏折叠、表高拖动、缩略图定位、空格确认并下一枚、待复核/自动切分筛选、确认状态与来源分离、参数过期提示。
- 单组撤销：9候选恢复为6候选；整图前景像素逐一相等；该组确认清空，撤销后恢复9候选和原来的4项确认。
- 项目ZIP内包含4个可编辑排版对象；独立空工作台重新打开恢复布局、标签、确认状态和来源关联；本地IndexedDB重启恢复同样通过，取消确认触发旧版面提示。
- 1440×960工作台、排版和1040×720小窗口截图已实际查看。提示改为底部状态栏，右侧确认列固定；最小窗口运行按钮可见。
- 原有13组浏览器检查通过：已知几何面积/校准、删除/撤销、切开、合并、勾选、排版编辑、CSV、mask/crop/cutout、项目恢复、单页TIFF。测试在独立浏览器上下文进行，不使用用户浏览器数据。
- 另行复测已校准对象的统一倍率按钮、单独缩放后的模式切换，以及Image026自动切分原有端到端检查（4枚独立mask/cutout、像素守恒、合并/撤销、项目恢复），均通过。
- 验证用项目中的勾选仅为交互测试，不是用户或专家对骨针完整性的科学确认。原始照片未修改。
- 构建目标为macOS Apple Silicon。Windows/Intel Mac和原生WebView中的恢复行为未实机验证；浏览器验证不等同于全平台验证。自动恢复是本地最近副本，不代替手动文件备份。

## 0.4.0 自动切分验证（2026-09-21）

- 30项单元测试通过：新增接触分水岭、面积回退、边界保护、撤销确认状态及8份真实mask回归夹具。
- 13项原有浏览器交互回归检查通过；新增Image026原JPEG端到端检查通过：6个连通候选→9个候选，其中指定接触组合分为4枚；四枚mask并集与同浏览器切分前原组合逐像素相等，单枚cutout、勾选、独立排版、撤销和项目恢复通过。
- 75次正式参数评估（74个不同组合），训练20个候选、留出14个候选，参数先冻结再检查留出。最优测试设置0.30/0.25；训练数量和标记点检查20/20，留出数量12/14，保留Image016漏切和误切失败。
- 全部30张有效照片：174→218个候选，29组发生切分；切分模块平均126 ms/图。每图重复运行逐像素一致，前景无新增/丢失。
- 参考仅为助手临时视觉判断，不是专家金标准；无IoU/AP，不宣称通用最优或标本外部验证。GUI与Pillow解码路径的Image026组合面积相差5 px，守恒比较使用同一路径输入。
- 默认保持完整性人工勾选；自动切线不保证真实边界，不重建遮挡。未勾选对象不进入结果和排版。

## 0.3.0 更新验证（2026-09-21）

- 19项单元测试通过：新增接触组合保留、复核提示、排版边界、单对象居中、200对象自动排列与版面格式验证。
- 13项浏览器检查通过：表格右侧逐行勾选、导出过滤、自动排版、拖动/撤销、旋转、缩放、标签、JSON恢复及2400×1800 PNG导出；原有测量、校准、mask编辑、项目恢复、TIFF流程仍通过。
- Image026独立测试：保留6个候选连通对象，排除3个贴边对象。用户截图中的四枚接触组合保留为#5，未自动拆分，测量为整个组合的投影；组合裁剪和排版可用。
- 原始照片只读。交付的Image026项目全部未勾选，不冒充逐枚人工审核；截图中的勾选仅用于界面功能测试。
- macOS arm64构建；Windows与Intel Mac未实机验证。此轮不重新报告整批30张的精度，且没有人工金标准。
- 排版为独立JSON快照，显示缩放非统一倍率；保存项目不保存版面，原图mask编辑不自动同步已有版面。

## 历史记录：0.1.0

日期：2026-09-21。第一版0.1.0，macOS Apple Silicon本机。

## 已完成

- TypeScript检查与Vite生产构建通过。
- Rust/Tauri `cargo check --offline`通过。
- Vitest：8项算法测试通过，覆盖矩形面积与Feret径、边缘排除、填孔、切开后ID、圆形近似圆度、渐变背景、亮目标、空白视野。
- Chrome/Playwright：合成演示成功分割；已知矩形面积为480与600 px²。
- 100 px校准线对应50 μm后，480 px²变为120 μm²。
- 删除和撤销、切开和撤销、多选合并和撤销正常。
- CSV包含物理单位和人工morphotype备注。
- ZIP包含原图、实例栅格、crop/mask/cutout、校准和来源字段。
- 保存后重新打开项目，恢复实例、尺度与备注。
- 单页TIFF夹具成功导入、分割。
- 上述浏览器测试未捕获运行时异常。
- 安装依赖时npm审计报告0个已知漏洞；这不是全面安全审计。
- Apple Silicon Mac发布版成功打包并实际启动；原生界面完成合成图分割、真实Image001导入及默认/调整参数分割，保存对话框成功写入项目ZIP，ZIP完整性和原图哈希检查通过。
- MR0145真实照片：31张文件中30张严格解码成功，使用相同处理核心比较3组参数，共90次；Image029解码异常且底部灰块，排除。默认/大窗口/全局参数分别生成767/250/225个候选区域，不能解释为骨针真实数量。详见交付目录`MR0145-test/测试报告.md`。

## 不应据此声称

- 尚无真实骨针人工标注集，不能报告生物学分类精度或分割IoU。
- 合成示例不是论文图，也不是真实实验结果。
- 没有自动物种或morphotype分类模型。
- 未验证所有TIFF压缩、位深、色彩模型；当前工作图为8位RGBA。
- Windows/Intel Mac构建、云端CI、开发者签名及公证尚未验证。
- 没有超大图、多小时压力测试或跨仪器泛化验证。

## 下一轮最小测试集

收集20—30张真实光镜原图，覆盖明背景/暗背景、均匀/不均匀照明、分离/粘连、清晰/失焦；优先保留原始分辨率和尺度来源。对其中数张逐枚人工标记，分别计算漏检、误检、合并/拆分错误及尺寸误差。仅在简单分割达不到需求时再增加模型。
